interface:
  display_name: "Motia"
  short_description: "Repo-specific patterns and workflows for motia"
  default_prompt: "Use the motia repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true